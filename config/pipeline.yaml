environment: dev

file_paths:
  raw_data: data/raw/
  staging_data: data/staging/
  cleaned_data: data/cleaned/
  reports: reports/

pipeline:
  batch_size: 1000
  fail_on_error: false
  write_invalid_records: true

logging:
  level: INFO
  log_file: data_summary.log
